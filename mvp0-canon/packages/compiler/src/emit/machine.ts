import type { ComponentSpec } from "../../../../spec/schema/component.schema.js";

export function emitMachine(_spec: ComponentSpec, _outDir: string): void {
  throw new Error("emitMachine: not implemented yet (Fase 3)");
}
